# Environment variables for backend services connecting to MongoDB

# Canonical MongoDB connection string (driver-friendly)
# Use authSource=admin because the user is created in the admin database
MONGODB_URL="mongodb://appuser:dbuser123@localhost:5000/?authSource=admin"

# The application database name
MONGODB_DB="myapp"
